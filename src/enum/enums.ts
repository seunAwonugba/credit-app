export enum TransactionType {
  DEBIT = 'debit',
  CREDIT = 'credit',
}
export enum Status {
  SUCCESS = 'success',
  FAILED = 'failed',
  PENDING = 'pending',
}
export enum Action {
  FUND = 'fund',
  TRANSFER = 'transfer',
  WITHDRAW = 'withdraw',
}
